# Private ebook files

Place the PDF here (gitignored):

`karykartyachi-ai-diary.pdf`

Or set `EBOOK_PDF_URL` in env to a private remote URL.
The download API streams the file only after a valid paid token check
(max 5 downloads, 72h expiry).
